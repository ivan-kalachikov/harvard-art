import { Routes } from '@/types/routes';

const routes: Routes = {
  century: '/century',
  culture: '/culture',
  object: '/object',
  technique: '/technique',
  period: '/period',
  place: '/place',
  color: '/color',
  worktype: '/worktype',
  classification: '/classification',
};

export default routes;
